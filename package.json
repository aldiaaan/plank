{
  "name": "plank",
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev codegen:watch --filter=@plank/web --filter=@plank/api --filter=@plank/client --filter=@plank/db --parallel",
    "codegen": "turbo run codegen --filter=@plank/client",
    "codegen:watch": "pnpm --filter @plank/client codegen:watch"
  },
  "keywords": [],
  "author": "Aldian",
  "license": "MIT",
  "packageManager": "pnpm@11.11.0",
  "devDependencies": {
    "turbo": "^2.10.4"
  }
}
